# 差距修复计划 — 前端缺口 + 后端清理

> **For agentic workers:** Use subagent-driven-development. Opus 分析审查，Sonnet 执行。

**Goal:** 修复审计发现的 4 个前端缺口和 3 个后端问题，使系统达到可演示状态。

**Working directory:** `/Users/jimmy/Downloads/Skills-World/.worktrees/mvp-implementation/`

---

## 修复优先级

| # | 修复项 | 优先级 | 预估 Task 数 | 涉及 |
|---|--------|--------|-------------|------|
| F1 | 玩家间聊天 UI | P0 | 2 | 前端 |
| F2 | 深度蒸馏前端入口 | P0 | 2 | 前端 |
| F3 | 管理面板 UI | P1 | 4 | 前端 |
| F4 | 设置面板补全（LLM + 经济） | P2 | 1 | 前端 |
| B1 | 旧 forge_service 清理 | P1 | 1 | 后端 |
| B2 | Alembic 迁移脚本 | P2 | 1 | 后端 |
| B3 | OAuth state 迁移 Redis | P2 | 1 | 后端 |

---

## F1: 玩家间聊天 UI（2 Task）

后端已完整：PendingMessage 模型、PlayerChatService 4 路路由、WS handler player_chat/set_reply_mode。前端 ChatDrawer 只支持 NPC。

### F1-T1: 扩展 ChatDrawer 支持玩家对话

**Files:**
- Modify: `frontend/src/components/ChatDrawer.tsx`
- Modify: `frontend/src/services/ws.ts`
- Modify: `frontend/src/stores/gameStore.ts`

**要点：**
- gameStore 新增 `chatTarget: {type: "npc", slug: string} | {type: "player", userId: string, name: string} | null`
- ChatDrawer 根据 chatTarget.type 切换行为：
  - NPC：现有逻辑不变（start_chat → chat_msg → stream reply）
  - Player：发送 `{type: "player_chat", target_id, text}` WS 消息
- ws.ts 新增 `player_chat_reply` 消息处理：收到后追加到聊天记录
- 显示 `is_auto` 标记（"AI 代答"徽章）区分自动/手动回复

### F1-T2: GameScene 点击玩家触发聊天

**Files:**
- Modify: `frontend/src/game/GameScene.ts`
- Modify: `frontend/src/game/phaserBridge.ts`

**要点：**
- GameScene update() 中已有 NPC 近距离检测，新增对 other players 的同样检测
- 点击/交互在线玩家时，通过 phaserBridge emit `player:interact` 事件
- GamePage 监听该事件，设置 chatTarget = {type: "player", ...}，打开 ChatDrawer

---

## F2: 深度蒸馏前端入口（2 Task）

后端 `/forge/deep-start` + `/forge/deep-status` 已就绪。前端 ForgePage 只有 5 步引导和 QuickForge。

### F2-T1: ForgePage 新增"深度蒸馏"模式选择

**Files:**
- Modify: `frontend/src/pages/ForgePage.tsx`
- Modify: `frontend/src/services/api.ts`

**要点：**
- ForgePage 顶部新增模式切换：「快速炼化」/「深度蒸馏」两个 Tab
- 快速炼化 = 现有的 ForgeChat + QuickForge（不动）
- 深度蒸馏 = 新的 DeepForge 组件
- api.ts 新增 `deepForgeStart(token, {character_name, raw_text, user_material})` 和 `deepForgeStatus(token, forge_id)`

### F2-T2: DeepForge 组件（深度蒸馏 UI）

**Files:**
- Create: `frontend/src/components/forge/DeepForge.tsx`

**要点：**
- 输入区：角色名 + 素材文本（可选大段文本）
- "开始蒸馏"按钮 → 调用 `/forge/deep-start`
- 进度展示：轮询 `/forge/deep-status/{id}`，显示当前阶段（路由中 → 调研中 → 提取中 → 构建中 → 验证中 → 精炼中 → 完成）
- 每个阶段用图标 + 进度条表示
- 完成后展示三层人设预览（复用 ForgePreview 组件）

---

## F3: 管理面板 UI（4 Task）

32 个 admin API 端点全部就绪，前端零实现。

### F3-T1: Admin 路由 + 布局 + 仪表盘

**Files:**
- Create: `frontend/src/pages/AdminPage.tsx`（布局：左侧导航 + 右侧内容）
- Create: `frontend/src/components/admin/AdminSidebar.tsx`
- Create: `frontend/src/components/admin/DashboardPanel.tsx`
- Modify: `frontend/src/App.tsx`（添加 /admin 路由，admin 守卫）
- Modify: `frontend/src/services/api.ts`（admin API 函数）

**要点：**
- AdminPage：左侧 6 个导航（仪表盘/用户/居民/炼化/经济/系统），右侧内容区
- DashboardPanel：4 个指标卡（调 GET /admin/dashboard/stats）+ 健康状态灯（调 /health）
- 路由守卫：非 admin 用户访问 /admin 重定向到首页（检查 user.is_admin）

### F3-T2: 用户管理面板

**Files:**
- Create: `frontend/src/components/admin/UsersPanel.tsx`

**要点：**
- 表格：用户列表（调 GET /admin/users，支持分页/搜索）
- 每行操作：调整余额（弹窗）、封禁/解封、设为管理员
- 点击用户名展开详情（创建的居民数、对话数、交易数）

### F3-T3: 居民管理 + 炼化监控面板

**Files:**
- Create: `frontend/src/components/admin/ResidentsPanel.tsx`
- Create: `frontend/src/components/admin/ForgeMonitorPanel.tsx`

**要点：**
- ResidentsPanel：居民列表 + 筛选（类型/街区/状态）+ 编辑三层人设
- ForgeMonitorPanel：活跃炼化会话列表（调 GET /admin/forge/active）+ 历史记录

### F3-T4: 经济 + 系统配置面板

**Files:**
- Create: `frontend/src/components/admin/EconomyPanel.tsx`
- Create: `frontend/src/components/admin/SystemConfigPanel.tsx`

**要点：**
- EconomyPanel：统计卡片（调 /admin/economy/stats）+ 交易流水表 + 参数配置表单
- SystemConfigPanel：分组配置（LLM/热度/评分/街区），每组展开为表单，保存调 PUT /admin/config/{group}

---

## F4: 设置面板补全（1 Task）

### F4-T1: 添加 LLM 和经济设置分区

**Files:**
- Modify: `frontend/src/components/profile/SettingsPanel.tsx`
- Modify: `frontend/src/services/api.ts`

**要点：**
- LLM 分区（仅当系统开启 allow_user_custom_llm 时显示）：
  - 启用开关、API 格式选择、Base URL / Key / Model 输入
  - "测试连接"按钮（调 POST /settings/llm/test）
- 经济分区：显示当前余额 + 低余额提醒阈值输入
- api.ts 新增 `updateLLM()`, `testLLMConnection()`, `updateEconomy()`

---

## B1: 旧 forge_service 清理（1 Task）

### B1-T1: 清理遗留代码

**Files:**
- Modify: `backend/app/services/forge_service.py`
- Modify: `backend/app/routers/forge.py`

**要点：**
- `_sessions` 内存字典保留（现有的 5 步引导和 /forge/quick 还在用）
- 但在旧代码顶部添加注释标注已废弃，指向新的 `app/forge/pipeline.py`
- 确保旧端点（/forge/start, /forge/answer, /forge/quick）和新端点（/forge/deep-start, /forge/deep-status）共存不冲突
- 长期计划：旧端点全部迁移到新管线后删除

---

## B2: Alembic 迁移脚本（1 Task）

### B2-T1: 生成正式迁移

**Files:**
- Create: `backend/alembic/versions/003_foundation_upgrade.py`

**要点：**
- 涵盖所有 Plan 1 的模型变更：User 13 字段、Resident 3 字段、SystemConfig 表、ForgeSession 表、PendingMessage 表
- 支持 upgrade + downgrade
- 在 SQLite 和 PostgreSQL 上都能运行

---

## B3: OAuth state 迁 Redis（1 Task）

### B3-T1: LinuxDo state 存 Redis

**Files:**
- Modify: `backend/app/routers/auth.py`

**要点：**
- 将 `_oauth_states` 内存字典改为 Redis（key: `oauth_state:{state}`, TTL: 600s）
- 如果 Redis 不可用，降级为内存字典（保持开发环境可用）
- 使用 `app/config.py` 中已有的 `redis_url` 配置

---

## 执行顺序

```
Phase A（可并行）:
├── F1-T1 + F1-T2（玩家聊天 UI）
├── F2-T1 + F2-T2（深度蒸馏入口）
└── F4-T1（设置补全）

Phase B（Phase A 完成后）:
├── F3-T1（Admin 布局 + 仪表盘）
├── F3-T2（Admin 用户管理）
├── F3-T3（Admin 居民 + 炼化）
└── F3-T4（Admin 经济 + 系统）

Phase C（独立，随时可做）:
├── B1-T1（旧代码清理）
├── B2-T1（Alembic 迁移）
└── B3-T1（Redis state）
```

---

## 总计

| 类型 | Task 数 | 新建文件 | 修改文件 |
|------|---------|---------|---------|
| 前端修复 | 9 | 8 | 10 |
| 后端清理 | 3 | 1 | 3 |
| **合计** | **12** | **9** | **13** |
